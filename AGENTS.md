# CommerceResolve 开发约定

## 事实来源

项目文档按以下优先级管理，不在多个文件重复维护同一事实：

1. `PROJECT_GOAL_MAP.md`：长期产品方向、版本顺序和当前焦点。
2. `docs/specs/system-constraints.md`：所有版本都不能绕过的业务与安全边界。
3. `docs/specs/vX.Y-*.md`：当前版本的用户行为、范围和验收条件。
4. `docs/plans/vX.Y-*-plan.md`：实现方案、垂直任务和验证命令。
5. `docs/adr/NNNN-*.md`：影响长期维护且难以撤销的技术决策。

发生冲突时先修正文档，不通过代码默默改变产品行为。

## 开发流程

每个版本遵循以下流程：

```text
目标地图
→ Feature Spec
→ 技术 Plan
→ 必要的 ADR
→ 垂直任务
→ 实现与测试
→ Eval 与版本验收
```

- Spec 只定义用户结果、范围、非目标和验收条件，不提前规定实现细节。
- Plan 只能基于已接受的 Spec 创建。
- ADR 只记录跨版本、影响长期架构或迁移成本较高的决策。
- 任务按可观察的端到端行为拆分，不按 Controller、Service、Database 等技术层横向拆分。
- 同一时间只推进一个版本；当前范围见 `PROJECT_GOAL_MAP.md`。

## 开始条件

任务开始前必须明确：

- 用户或系统可观察的结果。
- 包含与不包含的范围。
- 输入、输出、错误和权限边界。
- 自动化测试或明确的手工验收方式。
- 对现有 State、Checkpoint、工具契约和数据的兼容影响。

如果这些内容缺失，先完善 Spec 或 Plan，不直接实现。

## 实现原则

- 编写解决当前验收条件所需的最少代码。
- 所有新增或修改的函数、方法和 LangGraph 节点必须包含简洁的中文说明：Python 使用 docstring，TypeScript/JavaScript 使用 JSDoc；至少说明职责，参数、返回值、副作用、幂等性或异常语义不直观时必须一并说明。必要的行内注释也使用中文，但代码标识符、协议字段和标准技术术语保持原样。不得用逐行注释复述显而易见的代码。
- 业务规则、权限和金额计算必须是确定性代码，不能只存在于 Prompt 中。
- LLM 只能理解诉求、提取结构化信息和提出候选动作，不能自行授权资金操作。
- 工具必须职责单一，使用结构化输入输出，并可由 Fake 实现替换。
- LangGraph 节点返回 State 增量，不原地修改传入 State。
- State 不保存密钥、数据库连接、不可序列化对象或可重新读取的大型正文。
- Checkpoint、业务数据库、RAG 索引和审计数据必须分离。
- 不提前引入多 Agent、微服务、消息队列或向量数据库。
- 不修改与当前任务无关的代码或文档。

## 测试与 Eval

按风险采用以下验证层级：

- 单元测试：Schema、Reducer、Policy 和纯业务规则。
- 图测试：节点更新、路由、暂停、恢复和停止条件。
- 工具契约测试：参数、返回值、错误和幂等行为。
- 集成测试：临时数据库中的会话隔离和跨进程恢复。
- Eval：固定业务场景的工具选择、参数正确性、任务结果和安全违规。

测试默认使用 Fake Model 和 Fake 业务适配器，不依赖网络、真实 API Key 或真实退款系统。断言结构化 State 和业务结果，不断言模型完整措辞。

## 完成标准

任务完成必须同时满足：

- 当前验收条件全部通过。
- 相关自动化测试通过。
- 至少一条端到端路径完成验证。
- 高风险动作没有未经批准的副作用。
- 本次新增或修改的 Python 函数、方法和 LangGraph 节点均已补充中文 docstring；TypeScript/JavaScript 函数、组件和 Hook 均已补充中文 JSDoc。
- 必要文档、运行命令和已知限制已更新。
- 新增、删除、移动代码目录或改变文件职责时，已同步更新 `docs/codebase-guide.md`。
- 没有夹带无关重构或生成物。

版本完成还必须提供固定 Eval 数据集和可复现结果。

## Git 与仓库约定

- 默认分支为 `master`。
- 运行时数据库、索引、日志、缓存和密钥不提交仓库。
- 提交只包含一个明确目的，提交信息说明结果而非操作过程。
- 未经明确要求不自动提交、推送、创建 PR 或修改远端配置。
